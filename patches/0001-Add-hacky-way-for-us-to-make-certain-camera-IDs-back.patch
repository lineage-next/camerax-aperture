From 40658a6bbe894ac5a1ef321bb8584f11322d64ba Mon Sep 17 00:00:00 2001
From: LuK1337 <priv.luk@gmail.com>
Date: Mon, 17 Oct 2022 19:03:07 +0200
Subject: [PATCH] Add hacky way for us to make certain camera IDs backward
 compatible

Change-Id: If92f6869b3106202c609f8e22ab004af278a309d
---
 .../camera2/internal/CameraCompatibilityFilter.kt      | 10 ++++++++++
 1 file changed, 10 insertions(+)

diff --git a/camera/camera-camera2/src/main/java/androidx/camera/camera2/internal/CameraCompatibilityFilter.kt b/camera/camera-camera2/src/main/java/androidx/camera/camera2/internal/CameraCompatibilityFilter.kt
index a2a0ff91837..7ec3a025db6 100644
--- a/camera/camera-camera2/src/main/java/androidx/camera/camera2/internal/CameraCompatibilityFilter.kt
+++ b/camera/camera-camera2/src/main/java/androidx/camera/camera2/internal/CameraCompatibilityFilter.kt
@@ -30,6 +30,8 @@ import androidx.camera.core.InitializationException
  */
 public object CameraCompatibilityFilter {
 
+    private var backwardCompatibleCameraIds = listOf<String>()
+
     @JvmStatic
     public fun getBackwardCompatibleCameraIds(
         cameraDevices: CameraDevices,
@@ -56,6 +58,11 @@ public object CameraCompatibilityFilter {
         return backwardCompatibleCameraIds
     }
 
+    @JvmStatic
+    public fun setBackwardCompatibleCameraIds(cameraIds: List<String>) {
+        backwardCompatibleCameraIds = cameraIds
+    }
+
     @JvmStatic
     public fun isBackwardCompatible(cameraId: String, cameraDevices: CameraDevices): Boolean {
         // Always returns true to not break robolectric tests because the cameras setup in
@@ -67,6 +74,9 @@ public object CameraCompatibilityFilter {
             }
             return true
         }
+        if (backwardCompatibleCameraIds.contains(cameraId)) {
+            return true
+        }
         try {
             val cameraMetadata = checkNotNull(cameraDevices.awaitCameraMetadata(CameraId(cameraId)))
             val availableCapabilities =
-- 
2.51.0

